const styles = theme => ({
  root: {
    display: 'flex',
    padding: `${theme.typography.pxToRem(40)} ${theme.typography.pxToRem(30)}`,
  },
  mainSection: {
    flex: 1,
  },
});

export default styles;
